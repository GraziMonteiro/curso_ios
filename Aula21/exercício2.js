
    let nome = 'Graziela'
    let peso = 83.60
    let altura = 1.74
    let imc = peso / (altura * altura)

    const js = `${nome} tem o peso de ${peso} e a altura ${altura}, então seu ${imc}`

    console. log (js);